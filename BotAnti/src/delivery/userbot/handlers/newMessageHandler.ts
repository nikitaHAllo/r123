/**
 * Обработчик новых сообщений (userbot).
 * Регистрация обработчиков — в registerUserbotHandlers (userbotHandlers.ts).
 */
export { registerUserbotHandlers } from '../../../handlers/userbotHandlers.js';
